from flask_mturk import app

if __name__ == '__main__':
    # app.run(debug=False)
    #app.run(debug=True, use_reloader=False)  # For working logging
    # app.run(debug=True) # For working reloader
    app.run()
